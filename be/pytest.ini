[pytest]
# Tests live under be/tests but import the app as `be.*`, so pytest must run with
# the repo root on sys.path. rootdir is be/, hence the ".." here.
pythonpath = ..
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
