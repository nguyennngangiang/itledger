# Test-only dependencies. Install alongside requirements.txt:
#   docker compose exec api pip install -r be/requirements-dev.txt
# httpx is already a runtime dep (be/llm.py) and doubles as the ASGI test client.
pytest>=8.3
pytest-asyncio>=0.24
